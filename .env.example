# ---------------------------------------------------------------------------
# Eventerz — environment variables (copy to `.env.local` and fill in)
# All NEXT_PUBLIC_* vars are exposed to the browser. Keep secrets server-only.
# ---------------------------------------------------------------------------

# Site
NEXT_PUBLIC_SITE_URL="https://eventerz.xyz"

# Solana / RPC (Helius)
NEXT_PUBLIC_SOLANA_NETWORK="mainnet-beta"
NEXT_PUBLIC_HELIUS_RPC_URL="https://mainnet.helius-rpc.com/?api-key=YOUR_KEY"
HELIUS_API_KEY="your-helius-api-key"

# Anchor program
NEXT_PUBLIC_EVENTERZ_PROGRAM_ID="Eventerz1111111111111111111111111111111111"

# Metaplex / cNFT
NEXT_PUBLIC_MERKLE_TREE_ADDRESS=""

# Supabase (indexing / off-chain metadata)
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""

# Analytics
NEXT_PUBLIC_ANALYTICS_ID=""

# Newsletter (e.g. Resend / ConvertKit)
NEWSLETTER_API_KEY=""

# ---------------------------------------------------------------------------
# Auth — social sign-in (currently SIMULATED client-side for the demo).
# To wire real providers, add NextAuth/Auth.js or Supabase Auth and fill these.
# ---------------------------------------------------------------------------
AUTH_SECRET=""
# Google OAuth (https://console.cloud.google.com)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
# Apple OAuth (https://developer.apple.com — Sign in with Apple)
APPLE_CLIENT_ID=""
APPLE_CLIENT_SECRET=""
# Email magic links (e.g. Resend)
EMAIL_SERVER=""
EMAIL_FROM="eventerz.web@gmail.com"
